apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.app_name }}
  namespace: {{ .Values.app_name }}
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  azureFile:
    secretName: azure-secret
    shareName: {{ .Values.file_share }}
    readOnly: false
  persistentVolumeReclaimPolicy: Retain
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.app_name }}
  namespace: {{ .Values.app_name }}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 5Gi