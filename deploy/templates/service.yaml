apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.name }}
  namespace: {{ $.Release.Namespace }}
spec:
  selector:
    app: {{ .Values.app.name }}
  ports:
  - name: http
    port: 80
    targetPort: 80